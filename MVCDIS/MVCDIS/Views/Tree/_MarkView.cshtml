@model MVCDIS.Models.iasaWorktypesTree


@Scripts.Render("~/bundles/jqueryval")
<script type="text/javascript">
    $(".range").keyup(function () {

        var id = $(this).attr('data');
        var val = parseInt($(this).val());

        if (val < 0 || val > 5) {


            $(this).addClass("error");

        }
        else {



            $(this).css('background', 'white');
            $(this).removeClass("error");
        }



    });


    $(".range").keydown(function (e) {
        // Allow: backspace, delete, tab, escape, enter and .
        if ($.inArray(e.keyCode, [46, 8, 9, 27, 13, 110, 188]) !== -1 ||
            // Allow: Ctrl+A
    (e.keyCode == 65 && e.ctrlKey === true) ||
            // Allow: home, end, left, right, down, up
    (e.keyCode >= 35 && e.keyCode <= 40)) {
            // let it happen, don't do anything
            return;
        }
        // Ensure that it is a number and stop the keypress
        if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
            e.preventDefault();
        }
    });
   


</script>

<header>
    <div class="icons">
        <i class="fa fa-edit"></i>
    </div>
    <h5>Диапазон оценки мероприятия</h5>

    <!-- .toolbar -->
    <div class="toolbar">
        <nav style="padding: 8px;">


        </nav>
    </div>
    <!-- /.toolbar -->
</header>
<div style="overflow-y: auto;" aria-expanded="true" id="div-1" class="body">
    <fieldset>
        @using (Ajax.BeginForm("MarkView", "Tree", new AjaxOptions { UpdateTargetId = "targetid" }))
        {
            @Html.HiddenFor(x=>x.WorktypeId)
            @Html.Label("Минимальная оценка", new { @class = "control-label col-md-2" })
            @Html.TextBoxFor(x => x.MinGrade, new { @class = "form-control range" })
            @Html.ValidationMessageFor(x => x.MinGrade)
            <br />
            @Html.Label("Максимальная оценка", new { @class = "control-label col-md-2" })
            @Html.TextBoxFor(x => x.MaxGrade, new { @class = "form-control range" })
            @Html.ValidationMessageFor(x => x.MaxGrade)
              <br />
            <input class="btn btn-primary" type="submit" id="validsave" value="Сохранить">
            <br />
        }
        <p>







        </p>
        <div id="targetid" class="alert-success"></div>
    </fieldset>

</div>




