@model IEnumerable<MVCDIS.Models.LRoles>
@using MvcContrib;
@using MVCDIS.Models;
@*@using MvcContrib.UI.Grid;
    @using MvcContrib.UI.Grid.ActionSyntax;*@
@{
    ViewBag.Title = "Распределение ролей";
}



<style>
    #example {
        width: 100%;
    }



    table {
        table-layout: fixed;
    }

    td {
        overflow: hidden;
        text-overflow: ellipsis;
        word-wrap: break-word;
    }
</style>
@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('#example').dataTable({
                "iDisplayLength": 25,
                "bAutoWidth": false,
                "aaSorting": [[2, "asc"]],
                "oLanguage": {
                    "sLengthMenu": "_MENU_ Записей на странице"
                },
                "aoColumnDefs": [
         { 'bSortable': false, 'aTargets': [2] }
                ],
                "aoColumns": [{ "sWidth": "20%" }, { "sWidth": "10%" }, { "sWidth": "60%" }, { "sWidth": "10%" }]


            });
        });
    </script>
}

<div class="row">
    <br />
    <div class="alert alert-info">
        <strong><i class="fa fa-television fa-2x elementID" aria-hidden="true"><b>&nbsp;&nbsp;Управление интерфейсом</b></i></strong>
    </div>
    <div class="box dark">
        <header>
            <div class="icons">
                <i class="fa fa-edit"></i>
            </div>
            <h5>Таблица управления интерфейсом</h5>

            <!-- .toolbar -->
            <div class="toolbar">
                <nav style="padding: 8px;">
                    <a href="javascript:;" class="btn btn-default btn-xs collapse-box">
                        <i class="fa fa-minus"></i>
                    </a>
                    <a href="javascript:;" class="btn btn-default btn-xs full-box">
                        <i class="fa fa-expand"></i>
                    </a>

                </nav>
            </div>
            <!-- /.toolbar -->
        </header>
        <div style="overflow-y: auto;" aria-expanded="true" id="div-1" class="body">
            <fieldset>

                <table id="example" class="display" cellspacing="0" width="100%">
                    <thead>
                        <tr>

                            <th>Роль</th>
                            <th>События</th>
                            <th>Описание событий</th>

                            <th></th>

                        </tr>
                    </thead>
                    <tbody>

                        @foreach (MVCDIS.Models.LRoles item in Model)
                        {
                            <tr>
                                <td>@item.RoleName</td>
                                <td>
                                    @foreach (ActionList j in item.ActionAttending)
                                    {
                                        @j.Action <br />
                                    }
                                </td>
                                <td>
                                    @foreach (ActionList j in item.ActionAttending)
                                    {
                                        @j.Description <br />
                                    }
                                </td>

                                <td> @Html.ActionLink("Изменить", "ActionEdit", new { id = item.RoleId })</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                    </tfoot>


                </table>
            </fieldset>

        </div>
    </div>


</div>

@*@Html.Grid(Model).Columns(col =>
    {
        col.For(c => c.Lev).Named("Уровень");
        col.For(c => c.Name).Named("Название");
        col.For(c => c.CompetenceAttending.).Named("Компетенции");
        col.For(c => Html.ActionLink("Изменить", "Edit", new { id = c.ID })).Encode(false).Sortable(false);
        col.For(c => Html.ActionLink("Просмотр", "Details", new { id = c.ID })).Encode(false).Sortable(false);
        col.For(c => Html.ActionLink("Удалить", "Delete", new { id = c.ID })).Encode(false).Sortable(false);
    }).Attributes(id => "example")*@


