@model MVCDIS.Models.Competence

@{
    ViewBag.Title = "Редактировать данные выбранной компетенции";
}

<br />
<div class="alert alert-info">
    <strong><i class="fa fa-ellipsis-v fa-2x elementID" aria-hidden="true"><b>&nbsp;&nbsp;Компетенции</b></i></strong>
</div>
<div>

    @Html.ActionLink("Вернуться назад", "Index")
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />

        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            @Html.Label("Уровень компетенции", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("LevelID", null, String.Empty, new { @class = "form-control chzn-select-deselect" })
                @Html.ValidationMessageFor(model => model.LevelID)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Шифр", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
              @Html.TextBoxFor(model => model.Shifr, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Shifr)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextAreaFor(model => model.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Name)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Сохранить изменения" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

