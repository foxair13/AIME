@model MVCDIS.Models.iasaZvanie

@{
    ViewBag.Title = "Редактировать научное звание";
}
<br />
<div class="alert alert-info">
    <strong><i class="fa fa-graduation-cap fa-2x elementID" aria-hidden="true"><b>&nbsp;&nbsp;Научные звания</b></i></strong>
</div>
<div>

    @Html.ActionLink("Вернуться назад", "Index")
</div>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
       
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ZvanieId)

        <div class="form-group">
            @Html.LabelFor(model => model.ZvanieName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.ZvanieName, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ZvanieName)
            </div>
        </div>

        <div class="form-group">
            <div class="col-lg-6">
                <input type="submit" value="Сохранить изменения" class="btn btn-primary" />
            </div>
        </div>
    </div>
}
