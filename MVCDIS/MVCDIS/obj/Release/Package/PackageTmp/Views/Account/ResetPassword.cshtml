@model MVCDIS.Models.ResetPasswordModel

@{
    ViewBag.Title = "Сброс пароля";
}
<div id="login" class="animate form">
    <h1>Сброс пароля</h1>

    @using (Html.BeginForm("ResetPassword", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { Id = "resetForm" }))
    {
        <div id="HideReset">
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()
            <p>
                @Html.LabelFor(m => m.Password, new Dictionary<string, Object> { { "class", "youpasswd" }, { "data-icon", "p" } })
                @Html.PasswordFor(m => m.Password, new Dictionary<string, Object> { { "placeholder", "Введите ваш пароль" }, { "required", "required" } })
                @Html.ValidationMessageFor(m => m.Password)
            </p>
            <p>
                @Html.LabelFor(m => m.ConfirmPassword, new Dictionary<string, Object> { { "class", "youpasswd" }, { "data-icon", "p" } })
                @Html.PasswordFor(m => m.ConfirmPassword, new Dictionary<string, Object> { { "placeholder", "Повторите ваш пароль" }, { "required", "required" } })
                @Html.ValidationMessageFor(m => m.ConfirmPassword)
            </p>
            @Html.HiddenFor(m => m.ReturnToken)
            <p class="login button">
                <input id="ResetPassword" type="submit" value="Изменить" />
            </p>
        </div>
    }

    <div id="divReset" style='color: #ff0000; font-family: arial; font-size: large;'> </div>
    <p class="change_link">
        <a href="@Url.Action("Index", "Home")" class="to_register">Авторизация </a>
    </p>


</div>






