version: '3.4'

services:
  neftviewer.api:
    image: neftviewer.api:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8084:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
