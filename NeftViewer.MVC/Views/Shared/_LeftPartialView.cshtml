<style>
    .card {
        margin-right: 10px;
        margin-left: 10px;
    }

    .center-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
    }

    .card-body {
        padding: 0;
    }

    .full-width-row {
        margin-left: 0;
        margin-right: 0;
        width: 100%;
    }

    .fixed-width-dropdown {
        width: 200px;
    }

    #datepicker {
        text-align: center;
    }

    #amount {
        text-align: center;
    }

    .ui-slider .ui-slider-range {
        position: absolute;
        z-index: 1;
        font-size: .7em;
        display: block;
        border: 0;
        background-position: 0 0;
        background-color: #85a9ff;
    }

    #ui-datepicker-div {
        z-index: 10212 !important;
    }

    .daterangepicker {
        margin-left: 250px;
        z-index: 34523452345;
    }

</style>

<link href="@Url.Content("~/lib/daterangepicker/daterangepicker.min.css")" rel="stylesheet" />
<link href="@Url.Content("~/lib/datatables/jquery-3.7.0/jquery-ui-1.13.2.custom/jquery-ui.min.css")" rel="stylesheet" />
<div class="card shadow-none border border-300">
    <div class="card-header p-1 border-bottom border-300 bg-soft">
        <div class="row  justify-content-between align-items-center">
            <div class="col-12 col-md" align="center">
                <p class="text-900 mb-0">ВЫБОР РЕГИОНА</p>
            </div>
        </div>
    </div>
    <div class="p-2">
        <div class="row">
            <div class="col-md-12">
                <div class="card-body center-content">
                    <div class="p-2">
                        <select id="AreasID" class="form-select fixed-width-dropdown" aria-label="Выбор региона">
                            @foreach (var item in Model.AreasDropDown)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                    <div class="p-2">
                        <select class="form-select fixed-width-dropdown" id="ObjectID" aria-label="Выбор объекта">
                            @foreach (var item in Model.ObjectsDropDown)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
<hr />
<div class="card shadow-none border border-300">
    <div class="card-header p-1 border-bottom border-300 bg-soft">
        <div class="row  justify-content-between align-items-center">
            <div class="col-12 col-md" align="center">
                <p class="text-900 mb-0">ВЫБОР ПОН</p>
            </div>
        </div>
    </div>
    <div class="p-2">
        <div class="row">
            <div class="col-md-12">
                <div class="card-body center-content">
                    <select id="OwnersID" class="form-select fixed-width-dropdown" aria-label="Выбор ПОН">
                        @foreach (var item in Model.OwnersDropDown)
                        {
                            <option value="@item.Value">@item.Text</option>
                        }
                    </select>
                </div>
            </div>
        </div>
    </div>
</div>
<hr />
<div class="card shadow-none border border-300">
    <div class="card-header p-1 border-bottom border-300 bg-soft">
        <div class="row  justify-content-between align-items-center">
            <div class="col-12 col-md" align="center">
                <p class="text-900 mb-0">ТИП ОБЪЕКТА</p>
            </div>
        </div>
    </div>
    <div class="p-2">
        <div class="row">
            <div class="col-md-12">
                <div class="card-body center-content">
                    @foreach (var item in Model.TypeRadioOptions)
                    {
                        <div class="form-check">
                            <input class="form-check-input" type="radio" id="@item.Id" name="TypeRadio" value="@item">
                            <label class="form-check-label" for="@item.Id">@item.Value</label>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
<hr />


<div class="card shadow-none border border-300">
    <div class="card-header p-1 border-bottom border-300 bg-soft">
        <div class="row  justify-content-between align-items-center">
            <div class="col-12 col-md" align="center">
                <p class="text-900 mb-0">ПОКАЗАТЕЛИ</p>
            </div>
        </div>
    </div>
    <div class="p-2">
        <div class="row">
            <div class="col-md-12">
                <div class="card-body center-content">
                    <div class="accordion" id="accordion">
                        @foreach (var item in Model.CriteriaRadioModels)
                        {
                            <div class="accordion-item border-top border-300">
                                <h2 class="accordion-header" id='@String.Concat("heading_",item.AgregateId)'>
                                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target='@String.Concat("#collapse_",item.AgregateId)' aria-expanded="true" aria-controls='@String.Concat("collapse_",item.AgregateId)'>
                                        @item.AgregateName
                                    </button>
                                </h2>
                                <div class="accordion-collapse collapse" id='@String.Concat("collapse_",item.AgregateId)' aria-labelledby='@String.Concat("heading",item.AgregateId)' data-bs-parent="#accordion">
                                    <div class="accordion-body pt-0">
                                        @foreach (var typeitem in item.CriteriaRadioOptions)
                                        {
                                            <div class="p-1">
                                                <div class="card text-dark p-2">
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="radio" id="@typeitem.Id" name="CriteriaRadio" value="@typeitem.Value">
                                                        <label class="form-check-label" for="@typeitem.Id">@typeitem.Value</label>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<hr />




<script type="text/javascript" src="@Url.Content("~/lib/datatables/jquery-3.7.0/jquery-3.7.0.min.js")"></script>
<script src="@Url.Content("~/lib/knockout/knockout-3.5.1.js")"></script>

<script src="@Url.Content("~/lib/moment/moment-with-locales.min.js")"></script>
<script type="module" src="@Url.Content("~/lib/datatables/jquery-3.7.0/jquery-ui-1.13.2.custom/jquery-ui.min.js")"></script>
<script type="module" src="@Url.Content("~/lib/datatables/jquery-3.7.0/jquery-ui-1.13.2.custom/ui/i18n/datepicker-ru.js")"></script>
<script type="module" src="@Url.Content("~/lib/daterangepicker/daterangepicker.min.js")"></script>
<script type="text/javascript">
    $(document).ready(function () {

        moment.locale('ru');



        $("#slider-range").slider({
            range: true,
            min: 0,
            max: 0,
            values: [0, 0],
            slide: function (event, ui) {
                $("#amount").val("от: " + ui.values[0] + " до: " + ui.values[1]);

            },
            stop: function (event, ui) {

                OnUpdate();
            }
        });


        function toSec(dateString) {
            if (dateString.includes('-')) {
                var dateObject = new Date(dateString);
                var seconds = dateObject.getTime() / 1000;
                return seconds;
            } else if (dateString.includes('.')) {
                var parts = dateString.split(".");
                var year = parseInt(parts[0]);
                var month = parseInt(parts[1]) - 1;
                var day = parseInt(parts[2]);

                var dt = new Date(year, month, day);
                return dt.getTime() / 1000;
            } else {
                return NaN;
            }
        }
        $("#DateListTab").on('click', 'a.nav-link', function (event) {
            event.preventDefault();
            const tabId = $(this).attr('href').replace('#', '');
            handleTabChange(tabId);
            var objectIdValue = $("input[name='ObjectId']").val();
            var tabIdValue = $("input[name='TabId']").val();
            setActiveTab(tabIdValue, objectIdValue, 1);
            pagef = 1;
            OnUpdate();
        });
        $("#contactListTab").on('click', 'a.nav-link', function (event) {
           
            event.preventDefault();
            const tabId = $(this).attr('href');
            handleTabChange(tabId);
            var objectIdValue = $("input[name='ObjectId']").val();

          
            setActiveTab(tabId, objectIdValue, 1);
            pagef = 1;
            OnUpdate();
        });
        let isOnDate = false;
        function handleTabChange(tabId) {
            // Получаем ссылки на вкладки
            var chartsTab = document.querySelector('a[href="#Charts"]');
            var dashboardsTab = document.querySelector('a[href="#Dashboards"]');
            var objectParamsTab = document.querySelector('a[href="#ObjectParams"]');

            // Получаем ссылки на соответствующие элементы списка
            var chartsNavItem = chartsTab.parentElement;
            var dashboardsNavItem = dashboardsTab.parentElement;
            var objectParamsNavItem = objectParamsTab.parentElement;

            // Получаем соответствующие таб-панели
            var chartsTabPane = document.querySelector('#Charts');
            var dashboardsTabPane = document.querySelector('#Dashboards');

            var objectParamsTabPane = document.querySelector('#ObjectParams');

            if (tabId === 'OnDate') {

                isOnDate = true;
                chartsNavItem.hidden = true;
                dashboardsNavItem.hidden = true;
                var objectIdValue = $("input[name='ObjectId']").val();
                setActiveTab('#ObjectParams', objectIdValue, 1);
                pagef = 1;
       
                chartsTab.classList.remove('active');
                dashboardsTab.classList.remove('active');
                objectParamsTab.classList.remove('active');
                chartsTabPane.classList.remove('show', 'active');
                dashboardsTabPane.classList.remove('show', 'active');
                objectParamsTab.classList.add('active');
           
            
                objectParamsTabPane.classList.add('show', 'active');
             

            } else if (tabId === 'OnRangeDate') {
                isOnDate = false;
                var objectIdValue = $("input[name='ObjectId']").val();
                setActiveTab('#ObjectParams', objectIdValue, 1);
                pagef = 1;
                chartsNavItem.hidden = false;
                dashboardsNavItem.hidden = false;
            }
        }

        const activeTab = $('.nav-link.active');
        const activeTabId = activeTab.attr('href').replace('#', '');
        handleTabChange(activeTabId);
        function secToDate(s) {
            var dt = new Date(s * 1000);
            var year = dt.getFullYear();
            var month = dt.getMonth() + 1;
            var day = dt.getDate();
            var formattedDate = (day < 10 ? '0' : '') + day + '.' +
                (month < 10 ? '0' : '') + month + '.' +
                year.toString();
            return formattedDate;
        }
        function formatDate(date) {
            var year = date.getFullYear();
            var month = (date.getMonth() + 1).toString().padStart(2, '0');
            var day = date.getDate().toString().padStart(2, '0');
            return year + '.' + month + '.' + day;
        }
        function getCurrentYear() {
            var currentDate = new Date();
            return currentDate.getFullYear();
        }
        function getCurrentMonth() {
            var currentDate = new Date();
            return currentDate.getMonth() + 1;
        }
        function addLeadingZero(value) {
            return value < 10 ? '0' + value : value;
        }
        var currentYear = getCurrentYear();
        var currentMonth = getCurrentMonth();
        var startDay = '01';
        var endDay = new Date(currentYear, currentMonth, 0).getDate().toString();
        var startDate = currentYear + '.' + addLeadingZero(currentMonth) + '.' + startDay;
        var endDate = currentYear + '.' + addLeadingZero(currentMonth) + '.' + endDay;
        function formatDateToDdMmYyyy(date) {
            const dd = String(date.getDate()).padStart(2, '0');
            const mm = String(date.getMonth() + 1).padStart(2, '0');
            const yyyy = date.getFullYear();
            return dd + '.' + mm + '.' + yyyy;
        }
        $("#date-range").slider({
            range: true,
            min: toSec(startDate),
            max: toSec(endDate),
            step: 86400,
            values: [toSec(startDate), toSec(endDate)],
            slide: function (event, ui) {
                const minDate = new Date(ui.values[0] * 1000);
                const maxDate = new Date(ui.values[1] * 1000);

                $("#amountdate").val(formatDateToDdMmYyyy(minDate) + " - " + formatDateToDdMmYyyy(maxDate));

            },
            stop: function (event, ui) {
                const minDate = new Date(ui.values[0] * 1000);
                const maxDate = new Date(ui.values[1] * 1000);

        
                $(".daterangepicker-field").attr('data-start', formatDateToDdMmYyyy(minDate));
                $(".daterangepicker-field").attr('data-end', formatDateToDdMmYyyy(maxDate));
                sliderMinValue = formatDateToDdMmYyyy(minDate);
                sliderMaxValue = formatDateToDdMmYyyy(maxDate);
                var objectIdValue = $("input[name='ObjectId']").val();
                var tabIdValue = $("input[name='TabId']").val();
                setActiveTab(tabIdValue, objectIdValue, 1);
                pagef = 1;
                OnUpdate();
            }
        });
        $(".daterangepicker-field").daterangepicker({
            forceUpdate: true,
            locale: {
                format: 'DD.MM.YYYY',
                fromLabel: 'С',
                toLabel: 'По',
                customRangeLabel: 'Выбрать даты',
                weekLabel: 'Н',
                daysOfWeek: ['Вс', 'Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб'],
                monthNames: ['Январь', 'Февраль', 'Март', 'Апрель', 'Май', 'Июнь', 'Июль', 'Август', 'Сентябрь', 'Октябрь', 'Ноябрь', 'Декабрь'],
                firstDay: 1,
                yearSuffix: 'г.'
            },
            ranges: {
                'Последние 30 дней': [moment().subtract(29, 'days'), moment()],
                'Последние 90 дней': [moment().subtract(89, 'days'), moment()],
                'Свой выбор': 'custom'
            },
            callback: function (startDate, endDate, period) {
                var startDateText = startDate.format('YYYY.MM.DD');
                var endDateText = endDate.format('YYYY.MM.DD');
                var minSliderValue = secToDate($("#date-range").slider("option", "min"));
                var maxSliderValue = secToDate($("#date-range").slider("option", "max"));
                var secstartDate = toSec(startDateText);
                var secendDate = toSec(endDateText);
                var secminSliderValue = $("#date-range").slider("option", "min");
                var secmaxSliderValue = $("#date-range").slider("option", "max");

                if (secstartDate < secminSliderValue || secendDate >secmaxSliderValue) {
            
                    startDate = moment(minSliderValue, 'DD.MM.YYYY');
                    endDate = moment(maxSliderValue, 'DD.MM.YYYY');
                   
                    sliderMinValue = startDate.format('DD.MM.YYYY');
                    sliderMaxValue = endDate.format('DD.MM.YYYY');
                    var objectIdValue = $("input[name='ObjectId']").val();
                    var tabIdValue = $("input[name='TabId']").val();
                    setActiveTab(tabIdValue, objectIdValue, 1);
                    pagef = 1;

                    OnUpdate();
                }
                else {
                    var title = startDate.format('DD.MM.YYYY') + ' - ' + endDate.format('DD.MM.YYYY');
                    $(this).val(title);
                    $("#date-range").slider("values", [toSec(startDate.format('YYYY.MM.DD')), toSec(endDate.format('YYYY.MM.DD'))]);
                    sliderMinValue = startDate.format('DD.MM.YYYY');
                    sliderMaxValue = endDate.format('DD.MM.YYYY');
                    var objectIdValue = $("input[name='ObjectId']").val();
                    var tabIdValue = $("input[name='TabId']").val();
                    setActiveTab(tabIdValue, objectIdValue, 1);
                    pagef = 1;
                    OnUpdate();
                }


            }
        });

        $("#amountdate").val(secToDate($("#date-range").slider("values", 0)) +
            " - " + secToDate($("#date-range").slider("values", 1)));
        let allowedDates = [];
        $(function () {
            var today = new Date();
            var handle = $("#custom-handle");
            $("#amount").val("от: " + $("#slider-range").slider("values", 0) + " до: " + $("#slider-range").slider("values", 1));
            var dd = String(today.getDate()).padStart(2, '0');
            var mm = String(today.getMonth() + 1).padStart(2, '0');
            var yyyy = today.getFullYear();
            var currentDate = dd + '.' + mm + '.' + yyyy;
            $("#datepicker").attr("placeholder", currentDate);
            $("#datepicker").val(currentDate);
            $("#datepicker").datepicker({
                regional: $.datepicker.regional['ru'],
                onClose: function (selectedDate) {
                    OnUpdate();
                    var objectIdValue = $("input[name='ObjectId']").val();
                    var tabIdValue = $("input[name='TabId']").val();
                    setActiveTab(tabIdValue, objectIdValue, 1);

                },
                beforeShowDay: function (date) {
                    var formattedDate = $.datepicker.formatDate('dd.mm.yy', date);
                    return [allowedDates.indexOf(formattedDate) !== -1, ""];
                },
                beforeShow: function (input, inst) {
                    setTimeout(function () {
                        var windowWidth = $(window).width();
                        var windowHeight = $(window).height();
                        var inputWidth = $(input).outerWidth();
                        var inputHeight = $(input).outerHeight();
                        var inputOffset = $(input).offset();
                        var datePickerWidth = inst.dpDiv.outerWidth();
                        var datePickerHeight = inst.dpDiv.outerHeight();
                        var topMargin = 20;
                        var leftPosition;
                        var topPosition;
                        if (inputOffset.left + inputWidth + datePickerWidth <= windowWidth) {
                            leftPosition = inputOffset.left + inputWidth;
                        } else {
                            leftPosition = inputOffset.left - datePickerWidth;
                        }
                        if (inputOffset.top + inputHeight + datePickerHeight <= windowHeight) {
                            topPosition = inputOffset.top + inputHeight - 50;
                        } else {
                            topPosition = inputOffset.top - datePickerHeight - topMargin;
                        }

                        inst.dpDiv.css({
                            position: 'absolute',
                            left: leftPosition,
                            top: topPosition
                        });
                    }, 0);
                }
            });
        });
        var ownerId = $("#OwnersID").val();
        var areaId = $("#AreasID").val();
        var roadId = $("#RoadID").val();
        var objectId = $("#ObjectID").val();
        var IsBest = $('#IsBest').prop('checked');
        var EntriesID = $('#EntriesID').val();
        var Dates = $('#datepicker').val();
        var TypeValue = $("input[name='TypeRadio']:checked").attr('id');
        var CriteriaValue = $("input[name='CriteriaRadio']:checked").attr('id');
        var slideMin = $("#slider-range").slider("values", 0);
        var slideMax = $("#slider-range").slider("values", 1);
        $('#IsBest').change(function () {
            if (this.checked) {
                $('label[for="IsBest"]').text('ЛУЧШИЙ ОБЪЕКТ');
            } else {
                $('label[for="IsBest"]').text('ХУДШИЙ ОБЪЕКТ');
            }
            OnUpdate();
        });
        $('#EntriesID').change(function () {
            OnUpdate();
        });
        if ($('#IsBest').prop('checked')) {
            $('label[for="IsBest"]').text('ЛУЧШИЙ ОБЪЕКТ');
        } else {
            $('label[for="IsBest"]').text('ХУДШИЙ ОБЪЕКТ');
        }
        //getPointsForRegion(ownerId, areaId, TypeValue, roadId, objectId, IsBest, EntriesID, Dates, slideMin, slideMax, CriteriaValue, function (points) {
        //    addPointsToMap(points);
        //});
        function updateObjects(ownerId, areaId, TypeValue, roadId) {
            $.ajax({
                url: '../Home/GetObjects',
                method: 'GET',
                data: {
                    ownerId: ownerId,
                    areaId: areaId,
                    TypeValue: TypeValue,
                    roadId: roadId
                },
                success: function (data) {
                    var objectsDropdown = $("#ObjectID");
                    var currentSelectedValue = objectsDropdown.val();
                    objectsDropdown.empty();
                    $.each(data, function (index, item) {
                        objectsDropdown.append(new Option(item.text, item.value));
                    });
                    if (objectsDropdown.find("option[value='" + currentSelectedValue + "']").length) {
                        objectsDropdown.val(currentSelectedValue);
                    }
                }
            });
        }
        function OnUpdate() {
            var ownerId = $("#OwnersID").val();
            var areaId = $("#AreasID").val();
            var roadId = $("#RoadID").val();
            var TypeValue = $("input[name='TypeRadio']:checked").attr('id');
            var CriteriaValue = $("input[name='CriteriaRadio']:checked").attr('id');
            var objectId = $("#ObjectID").val();
            var IsBest = $('#IsBest').prop('checked');
            var EntriesID = $('#EntriesID').val();
            var Dates = $('#datepicker').val();
            var slideMin = $("#slider-range").slider("values", 0);
            var slideMax = $("#slider-range").slider("values", 1);

            updateObjects(ownerId, areaId, TypeValue, roadId);
            getPointsForRegion(ownerId, areaId, TypeValue, roadId, objectId, IsBest, EntriesID, Dates, slideMin, slideMax, CriteriaValue, function (points) {
                addPointsToMap(points);
            });
        }
        function updateDetailsForObject(objectId) {
            $.ajax({
                url: '../Home/GetDetailsForObject',
                method: 'GET',
                data: {
                    objectId: objectId
                },
                success: function (data) {
                    if (data.ownerId != null) {
                        $("#OwnersID").val(data.ownerId);
                    } else {
                        $("#OwnersID").val(0);
                    }
                    if (data.areaId != null) {
                        $("#AreasID").val(data.areaId);
                    } else {
                        $("#AreasID").val(0);
                    }
                    if (data.roadId != null) {
                        $("#RoadID").val(data.roadId);
                    } else {
                        $("#RoadID").val(0);
                    }

                },
                error: function (error) {
                    console.error("An error occurred:", error);
                }
            });
        }
        function updateequals(CriteriId) {
            $.ajax({
                url: '../Home/GetExtremumCriteria',
                method: 'GET',
                data: {
                    CriteriId: CriteriId
                },
                success: function (data) {
                    if (data.dateMin && data.dateMax) {
                        var minDateFormatted = data.dateMin;
                        var maxDateFormatted = data.dateMax;
                        var minDateSec = toSec(minDateFormatted);
                        var maxDateSec = toSec(maxDateFormatted);
                        $("#date-range").slider("option", "min", minDateSec);
                        $("#date-range").slider("option", "max", maxDateSec);
                        $("#date-range").slider("option", "values", [minDateSec, maxDateSec]);
                    }
                    $("#slider-range").slider("option", "values", [data.min, data.max]);
                    $("#slider-range").slider("option", "min", data.min);
                    $("#slider-range").slider("option", "max", data.max);
                    sliderMinValue = secToDate($("#date-range").slider("values", 0));

                    sliderMaxValue = secToDate($("#date-range").slider("values", 1));

                    $("#amountdate").val(secToDate($("#date-range").slider("values", 0)) +
                        " - " + secToDate($("#date-range").slider("values", 1)));
                    $("#amount").val("от: " + data.min + " до: " + data.max);
                    allowedDates = data.dates.map(date => $.datepicker.formatDate('dd.mm.yy', new Date(date)));
                    $("#datepicker").datepicker("option", "minDate", new Date(data.dateMin));
                    $("#datepicker").datepicker("option", "maxDate", new Date(data.dateMax));
                    OnUpdate();
                },
                error: function (error) {
                    console.error("An error occurred:", error);
                }
            });
        }
        $("input[name='CriteriaRadio']").change(function () {
            var CriteriId = $("input[name='CriteriaRadio']:checked").attr('id');
            $("#navbar-expand-rg-id").show();
            const navbarExpandRight = document.getElementById("navbar-expand-rg-id");
            const navbarExpandRightWidth = navbarExpandRight.clientWidth;
            contentElement.style.marginRight = navbarExpandRightWidth + "px";
            contentElement.classList.remove('collapsed');
            contentElement.classList.remove('collapsed-right');
            adjustModalWidth();
            updateequals(CriteriId);
        });


        $("input[name='AgregateCalc']").change(function () {
            OnUpdate();
        });

        $("input[name='TypeRadio']").change(function () {
            OnUpdate();
        });
        $(".form-select:not(#ObjectID, #EntriesID)").change(function () {
            OnUpdate();
        });
        $("#ObjectID").change(function () {
            OnUpdate();
        });
    });
</script>