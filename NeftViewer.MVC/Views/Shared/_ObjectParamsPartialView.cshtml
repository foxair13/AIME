@model IEnumerable<NeftViewer.Data.Models.IndicatorValue>

<div class="table-responsive">
    <table class="table table-bordered table-striped table-sm" style="width: 100%;">
        <thead class="thead-light">
            <tr>
                <th class="text-center fw-bold text-1100 fs--1 mb-0" style="width: 80%;">Показатель</th>
                <th class="text-center fw-bold text-1100 fs--1 mb-0" style="width: 20%;">@Model.Select(x => x.DateStart).FirstOrDefault().ToString("dd.MM.yyyy")</th>
            </tr>
        </thead>
        <tbody>
            @foreach (NeftViewer.Data.Models.IndicatorValue item in Model)
            {
                <tr>
                    <td class="text-center" style="width: 80%;">@item.Criterias.Name</td>
                    <td class="text-center fw-bold text-1100 fs--1 mb-0" style="width: 100%;">
                        @item.Value.ToString("0.00")
                      @*  <div class="progress bg-primary-100" style="height:3px;" role="progressbar">
                            <div class="text-center progress-bar bg-success" style="width: 100%;" role="progressbar" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
