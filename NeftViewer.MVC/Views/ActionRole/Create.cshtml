@model NeftViewer.MVC.Models.ActionRoleViewModel

@{
    ViewData["Title"] = "Добавление действий по ролям";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section HeadArea {
    <link href="@Url.Content("~/lib/Select2/dist/css/select2.min.css")" rel="stylesheet" />
}
<div class="card border border-primary">
    <div class="card-body">
        <h5 class="card-title">@ViewData["Title"]</h5>
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="ActionId" class="form-label"></label>
                @Html.DropDownListFor(m => m.ActionId, (SelectList)ViewBag.ActionId, new { @class = "form-control" })
                <span asp-validation-for="ActionId" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="RoleId" class="form-label"></label>
                @Html.DropDownListFor(m => m.RoleId, (SelectList)ViewBag.RoleId, new { @class = "form-control" })
                <span asp-validation-for="RoleId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Создать" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<a asp-action="Index" class="btn btn-link">Вернуться назад</a>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $("#ActionId").select2({
                theme: "classic"
            });
            $("#RoleId").select2({
                theme: "classic"
            });
        });
    </script>
    <script type="module" src="@Url.Content("~/lib/Select2/dist/js/select2.min.js")"></script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}