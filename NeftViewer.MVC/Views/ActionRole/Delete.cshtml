@model NeftViewer.MVC.Models.ActionRoleViewModel

@{
    ViewData["Title"] = "Удаление записи действия по назначенной роли";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card border border-danger">
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div class="card-body">
            <h5 class="card-title">@ViewData["Title"]</h5>
            <div class="mb-3">
                <label asp-for="ActionName" class="control-label"></label>
                @Html.DisplayFor(model => model.ActionName)
            </div>

            <div class="mb-3">
                <label asp-for="RoleName" class="control-label"></label>
                @Html.DisplayFor(model => model.RoleName)
            </div>

            <div class="form-group">
                <input type="submit" value="Удалить" class="btn btn-danger" />
            </div>
        </div>
    </form>
</div>
<a asp-action="Index" class="btn btn-link">Вернуться назад</a>