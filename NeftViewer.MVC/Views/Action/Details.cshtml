@model NeftViewer.MVC.Models.ActionViewModel

@{
    ViewData["Title"] = "Просмотр действия";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card border border-primary">
    <div class="card-body">
        <h5 class="card-title">@ViewData["Title"]</h5>
        <div class="mb-3">
            <label asp-for="Name" class="control-label"></label>
            @Html.DisplayFor(model => model.Name)
        </div>

        <div class="mb-3">
            <label asp-for="Description" class="control-label"></label>
            @Html.DisplayFor(model => model.Description)
        </div>

        <div class="form-group">
            <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model?.Id">Изменить</a>
        </div>
    </div>
</div>
<a asp-action="Index" class="btn btn-link">Вернуться назад</a>