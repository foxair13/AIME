# Отчёт о реализации

> Примечание: исходный `IMPLEMENTATION_REPORT.md` отсутствует в репозитории (как и
> `AI_SOCIAL_NETWORK_PLAN.md`, `IMPROVEMENT_PLAN.md`). Файл заведён заново; при появлении
> оригинала записи следует объединить.

---

## 2026-09-12 — Блок 1: исправление математики + метод Dynamic Fit Score

### Основание
Диссертация, гл. 2, с. 64–68 (рис. 1–5) и гл. 3 (формулы 3.1–3.3, 3.28–3.30).
Анализ: `docs/DYNAMIC_FIT_MATH.md`.

### Исправлены дефекты формул

| Формула | Дефект | Исправление |
|---|---|---|
| (3.2) `Normalize` | нет клампов, `Normalize(10,0,5)` возвращал `2.0` | кламп к [0..1]; вырожденный диапазон `max<=min` → 1 или 0 |
| (3.3) | нисходящая нормировка отсутствовала | добавлен `NormalizeDescending` |
| (3.1) `RoleFit` | веса не приводились к Σ=1, результат мог превышать 1 | нормировка весов, отсечение отрицательных |
| (3.28) | смешана с забыванием | выделен `LearningCurve` |
| (3.29) | отсутствовала | добавлен `DaysToReach` (обратная к 3.28) |
| (3.30) `CurrentLevel` | **семантическая инверсия**: уровень рос от бездействия | добавлен `DecayedLevel` (падение к floor); старый метод помечен `[Obsolete]` |

Публичный контракт не сломан: `CurrentLevel` сохранён и делегирует в `DecayedLevel`.

### Добавлено: Dynamic Fit Score (DFS)

Реализована модель контура управления со с. 66 (ПИ-регулятор: мотивация — пропорциональная
часть, компетентность — интегральная). Критерий отбора — площадь потерь эффективности
`J = ∫max(r−y(t),0)dt` (заштрихованная область рис. 1), а не статический балл.

Возможности: `Evaluate` (переходный процесс, время вхождения в должность, перерегулирование),
`Rank` (ранжирование по потерям на заданном горизонте), `RotationCost` (цена ротации в деньгах
с учётом разрыва первого рода).

### Изменённые файлы

| Файл | Изменение |
|---|---|
| `Application/Services/Services.cs` | `MathService`: 3.1–3.3, 3.28–3.30 |
| `Application/Contracts/IServices.cs` | `IMathService` расширен; добавлены `IDynamicFitService` и DTO-записи |
| `Application/Services/DynamicFitService.cs` | **новый** — модель контура |
| `Web/Controllers/DynamicFitController.cs` | **новый** — 3 эндпоинта |
| `Web/Program.cs` | DI: `IDynamicFitService` → Scoped |
| `Tests/DynamicFitServiceTests.cs` | **новый** — 8 тестов против рис. 4–5 |
| `Tests/MathServiceTests.cs` | тесты клампов, кривой обучения, затухания |

### Соответствие правилам
- DDD/SOLID: новый сервис отдельным файлом, `Services.cs` не переписывался.
- Слой Application не обращается к `DbContext`; `DynamicFitService` — чистые вычисления.
- Схема БД не менялась → миграции не требуются.
- Комментарии на русском, имена сущностей на английском.

### Верификация
`dotnet` в песочнице недоступен (нет egress к dot.net, установка SDK невозможна), поэтому
сборка и `dotnet test` **не запускались**. Вместо этого алгоритм портирован один в один на
Python и прогнан против всех утверждений тестов — 12/12 пройдено; поведение модели совпадает
с графиками рис. 4–5. Дополнительно проверена устойчивость ранжирования к шуму ±15 %
(200 прогонов).

**Требуется на машине с SDK:**
```
dotnet build AI.SocialNetwork.slnx
dotnet test  AI.SocialNetwork.slnx --no-build
```
Ожидание: 0 warnings / 0 errors. Возможное предупреждение CS0618 на устаревший `CurrentLevel`
подавлено тем, что вызовов не осталось — `GetCurrentLevelAsync` переведён на `DecayedLevel`.

### Не сделано
- Валидация модели на реальных данных наймов (нужны исторические данные компании).
- Калибровка параметров `Kкомпет`/`Tмотив` по методике измерения.

---

## 2026-09-12 — Прогон DFS на синтетической когорте

### Что сделано
Сгенерирована когорта 24 кандидатов (8 архетипов), прогнана через DFS, проведён бэктест
против независимо смоделированного «факта» (выгорание, текучесть, шум среды — эффекты,
неизвестные модели DFS).

### Инструменты (исследовательские, вне продакшн-контура)
| Файл | Назначение |
|---|---|
| `tools/dfs_sim.py` | порт `DynamicFitService` на Python 1:1 |
| `tools/gen_profiles.py` | генерация когорты + сравнение ранжирований |
| `tools/backtest.py` | бэктест против независимой «правды» |
| `tools/sensitivity.py` | скан по силе допущений (проверка на подгонку) |
| `tools/horizon.py` | скан по горизонту планирования |
| `tools/cohort.json` | сгенерированные данные |

### Результаты
- Пересечение топ-5 по скорингу и по DFS: **0 из 5**.
- Базовый сценарий: ρ(DFS)=+0,897 против ρ(скоринг)=−0,718; польза 23,25 против 10,37 (+124 %).
- **Ключевая оговорка:** при отключении выгорания и текучести DFS перестаёт работать
  (ρ=−0,099, выигрыш −0,4 %), а скоринг работает (ρ=+0,693). Всё преимущество метода
  держится на допущении, что перегрев и уход реальны и дороги.

Подробно: `docs/DFS_SIMULATION_RESULTS.md`.

### Статус
Валидация на реальных данных по-прежнему **не проведена**. Синтетика доказывает
самосогласованность модели, но не её прогностическую силу.

---

## 2026-09-12 — Прогон реального резюме через DFS

Скрипты: `tools/run_cv.py`, `tools/run_cv2.py`. Результат: `docs/CV_DFS_RUN.md`.

Выявлено методологическое ограничение метода: **DFS не умеет принимать на вход резюме.**
Параметры `Kкомпет`/`Tмотив` пришлось задавать экспертной догадкой, что воспроизводит ровно тот
недостаток классического скоринга, который метод критикует. Доказательную ценность имеет только
расчёт по фактическим датам (длительность мест работы против Тоб = 15 мес).

Вывод для продукта: до появления процедуры измерения параметров DFS применим к **историческим**
данным (где известен факт выхода на план), но не к скринингу резюме. Это уточняет пилот из
`MONETIZATION_PLAN.md`: продавать надо анализ уже нанятых, а не отбор кандидатов.

---

## 2026-09-12 — Блок 0 (частично): CI и гигиена секретов

### Сделано
| Изменение | Файл |
|---|---|
| GitHub Actions CI: restore → build `-warnaserror` → test → артефакт результатов; отдельная job сборки клиента | `.github/workflows/ci.yml` (новый) |
| Пароль PostgreSQL и ключ JWT убраны из конфигурации | `src/AI.SocialNetwork.Web/appsettings.json` |
| Шаблон переменных окружения | `.env.example` (новый) |
| `.env` и `appsettings.Development.json` добавлены в игнор | `.gitignore` |
| README переписан под внешнего читателя: значки, цифры проекта, раздел о матмодели, инструкция по секретам через user-secrets | `README.md` |

### Важно
Пароль `13d29e4A!` **остаётся в истории git**. Если репозиторий публичный, пароль нужно
считать скомпрометированным и сменить в локальной БД. Полная очистка истории —
`git filter-repo`, требует отдельного решения владельца (перезапись истории).

### Не проверено
CI не запускался: в песочнице нет .NET SDK. Первый прогон произойдёт при пуше в GitHub.
Возможные правки — версия SDK (`10.0.x`) и наличие `npm ci` при отсутствии `package-lock.json`
(предусмотрен фолбэк на `npm install`).
