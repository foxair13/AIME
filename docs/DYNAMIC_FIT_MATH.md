# Новая математика отбора кадров: Dynamic Fit Score (DFS)

Дата: 2026-09-12. Основание: диссертация, гл. 2, с. 64–68, рис. 1–5.
Статус: формулы исправлены, метод реализован и проверен.

---

## 1. Главная находка: вы искали не на тех страницах

Весь предыдущий аудит (`MVCDIS_FORMULA_AUDIT.md`) разбирал формулы главы 3 — (3.1) RoleFit,
(3.2)/(3.3) нормировка, (3.16)/(3.17) матрица переходов. Это **статические** формулы: они дают
балл в момент времени.

**Страницы 64–68 содержат принципиально другую математику, которой нет в коде вообще.** И именно
она — самая ценная часть работы. Там кадровый процесс описан не баллами, а **дифференциальным
уравнением контура управления**.

Формула (1) со с. 66:

```
dY(t)/dt = A(t)·Y(t) + B·X(t)
```

Это не «ещё одна метрика». Это смена парадигмы: **человек — не число, а звено системы
регулирования с переходным процессом.**

---

## 2. Что говорят графики

### Рис. 1 (с. 65) — суть идеи

График «Эффективность деятельности / Время» с тремя руководителями подряд. Ключевые элементы:

- при вхождении в должность есть **интервал разгона** — эффективность ниже требуемой;
- между руководителями — **разрывы первого рода** (замена);
- заштрихованная область подписана «**Потери эффективности из-за некомпетентности**».

**Вот это и есть настоящая целевая функция.** Не «балл кандидата», а **площадь потерь**.
Диссертация прямо рисует то, что нужно минимизировать, — и этой величины нет ни в одном
коммерческом продукте отбора персонала.

### Рис. 3 (с. 66) — структурная схема

Расшифрована по изображению:

```
x(t)=1(t) → [Σ] → e → ┬→ [Tмотив] ──┐
              ↑        └→ [Kкомпет/p] ┴→ y0 → [1/(1+Kэ·p)] → y1 → [1/(1+Tоб·p)] → y2 → [K] → y
              └──────────────── обратная связь по y ────────────────────────────────────┘
```

В терминах теории управления это **ПИ-регулятор**, где:

| Элемент | Роль в модели | Смысл в кадрах |
|---|---|---|
| `Tмотив` | пропорциональная часть | **мотивация** — реакция на разрыв «надо/есть» здесь и сейчас |
| `Kкомпет/p` | интегральная часть | **компетентность** — накопление результата, устраняет остаточную ошибку |
| `1/(1+Tэ·p)` | апериодическое звено | инерция экономики/среды |
| `1/(1+Тоб·p)` | апериодическое звено, Тоб=15 | инерция коллектива/объекта управления |

Это содержательно сильная идея: **мотивация даёт скорость, компетентность даёт точность.**

### Рис. 4 и 5 (с. 67) — численная валидация

Я восстановил модель и прогнал её. Сравнение с подписями к графикам:

**Рис. 4 — влияние мотивации (Kкомпет = 0,9):**

| Tмотив | Перерегулирование, % | t регулирования, мес |
|---|---|---|
| 1,0 | 52,3 | 54,4 |
| 3,0 | 25,4 | 17,9 |
| 5,0 | 13,4 | 16,0 |
| 10,0 | 3,1 | 3,0 |

**Рис. 5 — влияние компетентности (Tмотив = 3,0):**

| Kкомпет | Перерегулирование, % | t регулирования, мес |
|---|---|---|
| 0,9 | 25,4 | 17,9 |
| 0,7 | 19,9 | 20,1 |
| 0,4 | 9,1 | 23,6 |
| 0,1 | 0,0 | 46,1 |

**Совпадает с графиками качественно и по характеру кривых:** при малой мотивации — колебательный
процесс с большим выбросом (левый верхний график рис. 4 действительно осциллирует), при росте
мотивации процесс становится апериодическим; при падении компетентности выброс исчезает, но время
выхода на режим растёт втрое (правый нижний график рис. 5 — вялая экспонента). Модель
интерпретирована верно.

Проверялась и альтернативная трактовка (Tмотив как дифференцирующее звено) — она даёт
немонотонное перерегулирование, противоречащее рис. 4, и **отвергнута**.

---

## 3. Экономический смысл, которого не хватало

Диссертация на с. 68 даёт ключевую цифру: наличие разрывов и этапа вхождения в должность
**снижает качество управления в переходном режиме на 10–30 %**.

Это переводит модель в деньги. Если руководитель отвечает за поток стоимости `V` руб./мес, то
цена назначения:

```
Cost = V · [ Tвак·(r − эф_ио)  +  ∫₀^H max(r − y(t), 0) dt ]
        └── простой должности ──┘  └──── разгон нового ────┘
```

Моя симуляция вакансии при смене руководителя (потери за 120 мес):
вакансия 1 мес → −1,8 %; 3 мес → −3,0 %; 6 мес → −4,8 % средней эффективности.
На переходном участке падение соответствует заявленным 10–30 %.

**Это то, что нельзя купить ни в одном HR-продукте: цена кадрового решения в рублях.**

---

## 4. Метод Dynamic Fit Score

### Определение

Кандидат описывается парой параметров контура `(Kкомпет, Tмотив)` вместо одного балла.
Критерий отбора:

```
DFS = J = ∫₀^H max(r − y(t), 0) dt  →  min
```

где `y(t)` — решение системы (2) при начальных нулевых условиях.

**Побеждает не самый компетентный, а тот, кто даёт наименьшую площадь потерь за горизонт H.**

### Почему это даёт другой ответ, чем все существующие методы

Три кандидата, горизонт 36 месяцев:

| Кандидат | Kкомп | Tмотив | Статич. балл | Пик | t вхождения | **J (потери)** |
|---|---|---|---|---|---|---|
| A: «звезда», инертный | 0,90 | 1,0 | **высший** | 1,52 | 54,5 мес | **6,08** |
| B: середняк, мотивированный | 0,55 | 6,0 | средний | 1,03 | 5,3 мес | **2,28** ✅ |
| C: слабый, гиперактивный | 0,25 | 12,0 | низший | 0,99 | 3,1 мес | 2,59 |

**Ранжирование инвертируется.** Классический скоринг (и RoleFit, и любой ATS) выбирает A.
Динамическая модель показывает, что A — худший выбор: он **в 2,7 раза дороже** для организации,
потому что 4,5 года выходит на режим и по дороге даёт выброс 52 % (перерегулирование = метания,
избыточная активность, «новая метла»).

Проверка горизонтом: A не выигрывает **ни на одном** горизонте от 6 до 200 месяцев.

Проверка устойчивости (200 прогонов с шумом ±15 % по обоим параметрам):
победитель B — 148 раз, C — 52 раза, **A — 0 раз**. Вывод устойчив к погрешности оценки.

### Что метод даёт сверх «кто лучше»

1. **Время выхода на режим** — когда новичок начнёт окупаться (в месяцах, числом).
2. **Перерегулирование** — риск-метрика. Выброс > 30 % = кандидат будет «ломать через колено».
   Такого показателя нет нигде.
3. **Цена решения в рублях** — через `RotationCost`.
4. **Зависимость от горизонта** — под проект на 12 мес и под должность на 5 лет побеждают
   **разные** люди, и это считается, а не угадывается.

### Где такое уже применяют — и где нет

Аппарат ПИ-регулятора и интегральных критериев качества (ISE/IAE) — классика ТАУ с 1940-х, в
экономике применяется в макромоделях. **В отборе персонала — не применяется нигде:** HR-аналитика
(Skills Base, TalentGuard, Peoplebox, iMocha, Bryq) строит статические матрицы и предиктивные
скоринги, а не переходные процессы. Психометрика меряет черты, а не динамику вхождения в
должность.

Уникальность именно в **переносе аппарата ТАУ на кадровый отбор с денежной целевой функцией**.
Это защищаемая позиция: конкурент не скопирует, не имея модели.

---

## 5. Исправленные формулы

| Формула | Было | Стало |
|---|---|---|
| (3.2) `Normalize` | без клампов: `Normalize(10,0,5) = 2.0` | кламп в [0..1], вырожденный диапазон корректно |
| (3.3) убывающая | отсутствовала | `NormalizeDescending` |
| (3.1) `RoleFit` | веса не нормировались, результат уезжал > 1 | веса приводятся к Σ=1, результат в [0..1] |
| (3.28) обучение | смешано с забыванием | `LearningCurve` — рост к потолку |
| (3.29) скорость | отсутствовала | `DaysToReach` — обратная к (3.28) |
| (3.30) забывание | **уровень РОС от бездействия** | `DecayedLevel` — падает к остаточному floor |

Ошибка в (3.30) была грубой: `maxLevel − (maxLevel − currentLevel)·e^(−kt)` при `currentLevel <
maxLevel` **растёт** со временем. Навык без подтверждения «улучшался» сам. Теперь
`floor + (F₀ − floor)·e^(−λt)`.

Старый `CurrentLevel` помечен `[Obsolete]`, а не удалён, — публичный контракт не сломан.

---

## 6. Что добавлено в код

| Файл | Содержимое |
|---|---|
| `Application/Services/DynamicFitService.cs` | модель контура, `Evaluate`, `Rank`, `RotationCost` |
| `Application/Contracts/IServices.cs` | `IDynamicFitService`, `DynamicFitInput/Result`, `CandidateProfile`, `RankedCandidate` |
| `Application/Services/Services.cs` | исправленный `MathService` (3.1–3.3, 3.28–3.30) |
| `Web/Controllers/DynamicFitController.cs` | `POST /api/dynamicfit/evaluate`, `/rank`, `/rotation-cost` |
| `Tests/DynamicFitServiceTests.cs` | 8 тестов против рис. 4–5 |
| `Tests/MathServiceTests.cs` | тесты клампов, кривой обучения, затухания |

DI: `IDynamicFitService` зарегистрирован Scoped в `Program.cs`. DDD не нарушен — новый сервис в
отдельном файле, чистая функция без обращения к `DbContext`.

---

## 7. Ограничения — честно

1. **Параметры `(Kкомпет, Tмотив)` нужно чем-то измерять.** Это главный вопрос. Пути: калибровка
   по историческим данным компании (когда прошлые сотрудники выходили на план), оценка
   руководителем по шкале, тестирование. Без калибровки метод даёт относительное ранжирование,
   но не абсолютный прогноз.
2. **Тоб = 15 мес взято из диссертации** и должно настраиваться под отрасль.
3. **Модель линейна** — не описывает выгорание, конфликты, увольнение по своей воле.
4. **Валидация на реальных данных не проведена.** Модель самосогласована и воспроизводит графики
   диссертации, но прогностическая сила на живых наймах не измерена. Это следующий шаг и главный
   риск.

Пункт 4 — то, что превращает метод из красивого в продаваемый. Нужны исторические данные хотя бы
одной компании: 20–30 наймов с датами выхода на план.

---

## 8. Почему это меняет продуктовую позицию

В `HR_PLATFORM_ANALYSIS.md` вывод был: продавать аналитику, дифференциатор — формулы, которых нет
у конкурентов. Теперь дифференциатор стал сильно острее:

> **«Мы считаем не балл кандидата, а сколько он будет стоить компании за время выхода на
> результат — в рублях и месяцах.»**

Это продаётся руководителю без объяснения математики. И на вопрос «а чем вы лучше?» есть ответ,
который конкурент не повторит: у него нет модели переходного процесса.

Проверить гипотезу можно **до** написания продукта: взять у знакомой компании историю 20 наймов,
прогнать модель, сравнить прогноз с фактом. Если корреляция есть — это диссертация, превращённая
в бизнес.
