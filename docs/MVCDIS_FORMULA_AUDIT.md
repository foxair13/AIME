# Сверка формул MVCDIS с реализацией в коде

Источник: Лисичёнок С.И. «Модель и инструментальные средства управления карьерой и
формированием компетентности государственных служащих», Академия управления при Президенте
Республики Беларусь, Минск 2017. Науч. рук. Ивановский А.В. Главы 1 и 3, 176 страниц.

Проверено: `MathService`, `CompetenceService` (`Application/Services/Services.cs`),
`UserSkill`, `RoleSkillWeight` (Domain), `MathServiceTests`.

**Итог: одна формула (3.1) перенесена верно; в остальных — от неточностей до подмены решаемой
задачи. Два расхождения меняют поведение системы: кривая обучения (§3) и Branch&Bound (§5-бис).**

---

## Сводная таблица

| Формула | В диссертации | В коде | Вердикт |
|---|---|---|---|
| (3.1) рейтинг компетентности | `Zk(Ki) = Σ(Vi · xi)` | `RoleFit` — Σ(weights·xi) | ✅ **верно** |
| (3.2) нормировка, возрастающая | кусочная, с отсечками 0/1 | `Normalize` — без отсечек | ⚠️ **частично** |
| (3.3) нормировка, убывающая | `(MAXi − Ki)/(MAXi − MINi)` | **не реализована** | ❌ **отсутствует** |
| (3.27)–(3.28) кривая обучения | ОДУ и его решение | свёрнуто в одну формулу | ⚠️ **семантика искажена** |
| (3.29) коэффициент k | `k = −ln(1 − f(t)/f(t)max)/t` | **не реализована** | ❌ **отсутствует** |
| (3.30) уровень освоения | `f(t)i = Rimax − (Rimax − Ri)·e^(−kt)` | формально совпадает | ⚠️ см. §3 |
| (3.9) кластеризация | иерархическая, complete-linkage | K-Means | ❌ **другой алгоритм** |
| (3.5)–(3.7) z-нормировка | z-score перед кластеризацией | **не реализована** | ❌ **отсутствует** |
| (3.16)–(3.17) матрица затрат | попарные отношения весов | **не реализована** | ❌ отсутствует (ключевая) |
| Branch&Bound | задача **коммивояжёра** (порядок освоения) | задача **о рюкзаке** | ❌ **другая задача** |

---

## 1. Формула (3.1) — рейтинг компетентности ✅

**Диссертация, с. 80:**
```
Zk(Ki) = Σ (Vi · xi)
```
где `Vi` — весовой показатель компетенции **в процентах**, `xi` — нормированная оценка,
`Zk` — рейтинг **в процентах**.

**Код** (`MathService.RoleFit`) — реализовано верно.

**Замечание по единицам измерения.** В диссертации веса заданы в процентах и сумма
`Σ Vi = 100 %`, поэтому `Zk ∈ [0, 100]` (в таблице 3.5 значения 70.28 %, 76.8 %, 17.12 %).
В коде `RoleSkillWeight.Weight` откомментирован как «приоритет Vi, %», но нигде не проверяется,
что веса роли в сумме дают 100. Если администратор заведёт веса `[1, 1]`, RoleFit вернёт
`1.5` — что не процент и не доля. Тест `RoleFit_EqualWeights` этот случай фиксирует как
ожидаемое поведение (ожидает `1.5m`).

**Рекомендация:** либо нормировать веса на их сумму внутри `RoleFit` (тогда результат всегда
`[0..1]`), либо добавить валидацию `Σ Weight = 100` при заведении роли. Второе ближе к
первоисточнику. Это не баг сегодня, но гарантированный источник несопоставимых чисел между
ролями.

---

## 2. Формулы (3.2)/(3.3) — нормировка ⚠️ / ❌

**Диссертация, с. 80 — это кусочно-заданные функции, а не линейное отображение.**

Возрастающая (3.2):
```
xi = 0,                              если Ki ≤ MINi
xi = (Ki − MINi)/(MAXi − MINi),      если MINi ≤ Ki ≤ MAXi
xi = 1,                              если Ki > MAXi
```

Убывающая (3.3):
```
xi = 1,                              если Ki ≤ MINi
xi = (MAXi − Ki)/(MAXi − MINi),      если MINi ≤ Ki ≤ MAXi
xi = 0,                              если Ki > MAXi
```

**Код:**
```csharp
public decimal Normalize(decimal x, decimal min, decimal max)
{
    if (max == min) return 1m;
    return (x - min) / (max - min);   // отсечек нет
}
```

**Расхождения:**

1. **Нет отсечек (clamp).** При `x > max` результат превышает 1, при `x < min` — отрицателен.
   В `CompetenceService.GetRoleFitAsync` вызывается `Normalize(scores[i], 0m, maxScores[i])`
   с `scores = Math.Min(us.Level, 10)`. То есть уровень зажимается по 10, а `MaxScore` берётся
   из БД и может быть равен 5 (как в диссертации!). Тогда `Normalize(10, 0, 5) = 2.0` — вклад
   компетенции удваивается. **Это реальный дефект, воспроизводимый на данных из первоисточника.**

2. **`MinScore` игнорируется.** В `RoleSkillWeight` поле `MinScore` есть и откомментировано как
   «порог допуска (в MVCDIS ~4/5)», но `GetRoleFitAsync` передаёт в `Normalize` жёсткий `0m`.
   Порог допуска из модели просто не работает.

3. **Убывающая функция (3.3) не реализована.** Для соцсети она нужна не меньше возрастающей:
   «время отклика», «стоимость часа», «срок сдачи» — чем меньше, тем лучше. Сейчас такие
   характеристики выразить нечем.

4. **`max == min → 1m`** — произвольное решение, в первоисточнике этот случай не определён.
   Для возрастающей функции при `Ki = MINi = MAXi` по (3.2) корректнее 0 или 1 в зависимости от
   строгости неравенства; возврат 1 «за бесплатно» завышает оценку.

**Рекомендация:** реализовать обе кусочные функции честно, с отсечками:
`NormalizeAscending(x, min, max)` и `NormalizeDescending(x, min, max)`; в `GetRoleFitAsync`
передавать `rw.MinScore`, а не `0m`. Сигнатуру `Normalize` сохранить как обёртку над
возрастающей, чтобы не ломать существующие вызовы и тесты.

---

## 3. Формулы (3.27)–(3.30) — кривая обучения ⚠️ Главное расхождение

Здесь моё подозрение из прошлого сообщения подтвердилось, но **не так, как я предполагал**.
Честная формулировка: это не «четыре уравнения свёрнуты в одно», а **смысл функции подменён**.

**Что в диссертации (с. 101–103):**

(3.27) — дифференциальное уравнение скорости обучения:
```
f'(t) = df(t)/dt = k · (f(t)max − f(t))
```
Прямая цитата: *«производная от функции обучения f'(t) представляет собой показатель скорости
обучения, который прямо пропорционален объему необходимому для изучения материалу»*.

(3.28) — решение этого ОДУ:
```
f(t) = f(t)max − (f(t)max − f(T)) · e^(−k·t)
```

(3.29) — вычисление коэффициента скорости обучения из наблюдаемых данных:
```
k = −ln(1 − f(t)/f(t)max) / t
```

(3.30) — та же кривая в терминах уровней освоения компетенции:
```
f(t)i = Rimax − (Rimax − Ri) · e^(−k·t)
```

**Ключевой момент — что это за кривая.** В диссертации это **кривая ОБУЧЕНИЯ**: `t` — это
**затраченное на изучение время** (в таблице 3.17 коэффициенты k = 0.92, 1.61, 2.30 рассчитаны
по часам, потраченным на материал). Функция **растёт** от текущего уровня `Ri` к максимуму
`Rimax` по мере того, как человек **учится**. Результат (3.30) используется, цитата: *«Отсортировав
результаты расчета f(t)i в порядке возрастания, получим рациональную последовательность изучения
материалов»* — то есть это **инструмент планирования обучения**, ранжирования учебных материалов
по производительности.

**Что в коде:**
```csharp
// Кривая обучения/забывания (3.27–3.30): F(t) = Fmax − (Fmax − F0)·e^(−k·t)
// Здесь F0 = currentLevel, Fmax = maxLevel, k = decayRate, t = дни с последнего подтверждения
public decimal CurrentLevel(decimal maxLevel, decimal currentLevel, DateTime lastConfirmedAt,
                            decimal decayRate, DateTime now)
{
    var days = (decimal)(now - lastConfirmedAt).TotalDays;
    ...
    return maxLevel - (maxLevel - currentLevel) * decay;
}
```

Алгебраически формула та же. **Но подставлены другие величины:**

| | Диссертация (3.30) | Код |
|---|---|---|
| `t` | время, **потраченное на обучение** | дни **простоя** с последнего подтверждения |
| `k` | коэффициент **скорости обучения** (3.29) | `DecayRate` — «коэффициент устаревания» |
| смысл | рост компетенции при обучении | должно быть затухание при неиспользовании |

**Последствие — поведение прямо противоположно замыслу.** Функция монотонно **растёт** к
`maxLevel` с ростом `t`. В коде `t` = дни бездействия. Значит: **чем дольше пользователь не
подтверждает навык, тем выше становится его расчётный уровень.** При `t → ∞` уровень стремится
к 10 (максимуму) независимо от реального уровня.

Это подтверждается собственным тестом проекта — обратите внимание на его название:
```csharp
[Fact]
public void CurrentLevel_DecaysTowardMaxLevel()   // «затухает В СТОРОНУ максимума»
```
Тест зелёный, потому что фиксирует фактическое поведение. Название невольно описывает ошибку:
затухание не может быть направлено к максимуму.

Комментарий в коде «кривая обучения/**забывания**» тоже показателен: в диссертации сказано
*«Обучение это процесс усвоения и забывания учебной информации»*, но формулы (3.27)–(3.30)
описывают **только фазу усвоения**. Отдельного уравнения забывания в главе 3 нет — судя по
всему, автор кода добросовестно взял единственную имеющуюся формулу и попытался применить её к
задаче забывания, для которой она не предназначена.

**Рекомендация — разделить на две разные функции:**

1. `LearningCurve(rMax, r0, k, timeSpent)` — честная (3.28)/(3.30), для планирования развития:
   «сколько часов нужно вложить в навык, чтобы дойти до уровня X» и для сортировки материалов
   плана развития (`DevelopmentPlan`/`PlanStep` — сущности уже есть, формула их наполнит смыслом).
2. `LearningRate(f, fMax, t)` — формула (3.29), сейчас отсутствует; без неё `k` неоткуда взять,
   он задаётся руками в `UserSkill.DecayRate`.
3. `DecayedLevel(level, lastConfirmedAt, decayRate, now)` — **затухание**, которое и подразумевалось:
   `L(t) = Lmin + (L0 − Lmin)·e^(−λ·t)`, монотонно убывает к базовому уровню. Это классическая
   кривая забывания Эббингауза; в диссертации её нет, и это **наше расширение** — так и надо
   его задокументировать, не приписывая MVCDIS.

Менять сигнатуру `CurrentLevel` придётся — она сейчас семантически неверна. Это ломающее
изменение в `ICompetenceService`, поэтому нужно ваше согласие.

---

## 4. Кластеризация (3.9) — другой алгоритм ❌

**Диссертация, с. 83:** иерархическая агломеративная кластеризация с **complete linkage**:
```
D(X, Y) = max d(x, y),  x ∈ X, y ∈ Y
```
Цитата: *«Вначале каждый элемент матрицы считается отдельным кластером. В дальнейшем кластеры
последовательно объединяются… расстояние определяется как максимум из множества расстояний»*.
Результат — дендрограмма (рис. 3.25), группы резервистов по направлениям работы (табл. 3.8).

**В коде:** `KMeansCluster` — K-Means со случайной инициализацией.

Это **принципиально другой метод**: K-Means требует задать `k` заранее, даёт разные результаты
от запуска к запуску и не строит иерархию. Иерархическая кластеризация детерминирована, `k`
выбирается по дендрограмме постфактум.

**Практический вывод: ваш флейки-тест — следствие именно этой подмены.** `KMeans_SeparatesThreeDistinctGroups`
падает не из-за «неудачного seed», а потому что для задачи из диссертации выбран
недетерминированный алгоритм. Иерархическая кластеризация с complete-linkage на тех же данных
даёт один и тот же ответ всегда — тест перестанет быть флейки *по построению*, а не потому что
мы зафиксировали seed.

Отдельно: комментарий в коде утверждает «(k-means++)», хотя реализован выбор случайных точек.
k-means++ — это взвешенная по расстоянию инициализация; здесь её нет. Комментарий вводит в
заблуждение.

**Также не реализована z-нормировка (3.5)–(3.7)**, которую диссертация требует *до* расчёта
матрицы расстояний: *«сначала необходимо стандартизовать (нормировать) данные»*. Без неё
признаки с большим разбросом доминируют в евклидовом расстоянии.

**Рекомендация:** добавить `HierarchicalCluster(points, k, linkage)` по (3.9) + `ZScoreNormalize`
по (3.5)–(3.7). `KMeansCluster` не удалять (он полезен для больших объёмов, где иерархический
O(n³) неприменим), но сделать детерминированным через seed и исправить комментарий.

---

## 5. Матрица стоимости затрат (3.16)–(3.17) — не реализована, но важна ❌

**Диссертация, с. 93:**
```
Xij = Vi / Vj                    (3.16)  — попарное отношение весов компетенций
Rij = Xij · (Kri + Krj)          (3.17)  — стоимость затрат на пару компетенций
```
где `Kri`, `Krj` — суммы недостающих баллов до полного овладения компетенциями.

Дальше (3.18)–(3.25) на этой матрице строится **граф путей формирования компетенций** и
выбирается оптимальная траектория развития: *«Так как траекторий по формированию компетенций
может быть несколько, то они представляют собой граф путей по формированию компетенций»*.

**В коде этого нет вообще.** Между тем это — сердцевина практической ценности MVCDIS:
не «оценить человека», а **построить ему оптимальный маршрут развития** с учётом того, что
осваивать компетенции в разном порядке стоит по-разному.

Для соцсети это прямо ложится на существующие сущности `DevelopmentPlan` / `PlanStep`, которые
сейчас в модели есть, а содержательного алгоритма за ними нет. Это самая сильная незакрытая
идея из диссертации.

---

## 5-бис. Branch&Bound: в коде решается НЕ ТА ЗАДАЧА ❌ Критично

Это выяснилось при чтении эталонной реализации `MVCDIS/` (она теперь тоже в репозитории).

**Что делает MVCDIS** (`Models/BranchAndBoundManager.cs`, `Controllers/CalculateController.cs`):

Прямая цитата из комментария в исходнике MVCDIS:
```
/* Решает задачу "о коммивояжёре" методом "ветвей и границ".
   Matr - матрица весов в орграфе на N вершинах (0-нет ребра).
   Ans  - оптимальный путь в графе */
```

А вот как строится матрица (`CalculateController.cs`, строка ~643) — это **буквально формула
(3.17)** из диссертации:
```csharp
Graph[t + 1, m + 1] = Math.Round((TO[t].ValueComp / TO[m].ValueComp) * (TO[t].Value + TO[m].Value), 2);
//                                └────── Xij = Vi/Vj (3.16) ──────┘   └── (Kri + Krj) ──┘
```
где `TO[].Value = 5 − набранный балл`, то есть **недостающие баллы** `Kr`, как и описано на с. 94.

Итого MVCDIS: строит матрицу стоимостей (3.16)/(3.17) → решает на ней **задачу коммивояжёра**
методом ветвей и границ → получает **оптимальный порядок освоения компетенций** (`ShortPath`).

**Что делает наш код** (`MathService.BranchAndBoundKnapsack`):
```csharp
public KnapsackResult BranchAndBoundKnapsack(double[] weights, double[] values, double capacity)
// задача о рюкзаке 0/1: выбрать ПОДМНОЖЕСТВО максимальной ценности в пределах вместимости
```

**Это разные задачи.** Совпадает только метод решения («ветвей и границ»):

| | MVCDIS / диссертация | Наш код |
|---|---|---|
| Задача | коммивояжёр (TSP) | рюкзак 0/1 |
| Ответ | **порядок** обхода компетенций | **подмножество** элементов |
| Вход | матрица `Rij` попарных стоимостей | веса, ценности, вместимость |
| Смысл | «в какой последовательности учиться» | «что выбрать при ограничении» |

Комментарий в `IServices.cs` — «Branch&Bound — оптимальный подбор сделки/набор элемент
(задача о рюкзаке 0/1)» — честно описывает то, что написано, но это **не то, что делает MVCDIS**.
Похоже, при переносе сохранили название метода и потеряли задачу.

**Почему это важно, а не педантизм.** Порядок освоения компетенций — центральный результат
главы 3 (ради него и строится матрица (3.16)–(3.17), и пишутся страницы 93–99). Задача о рюкзаке
на эти данные не отвечает на вопрос «с чего начать и чем продолжить». Для соцсети это ровно
то, что должно наполнять `DevelopmentPlan`/`PlanStep`.

**Рекомендация:** реализовать `OptimalCompetencePath` (TSP методом ветвей и границ) в дополнение
к существующему рюкзаку — **не вместо**: рюкзак останется полезен для подбора набора сделок под
бюджет. В MVCDIS есть рабочий, проверенный на практике алгоритм (527 строк) — его логику можно
перенести, переписав в современном C# и покрыв тестами; сверять результат удобно по табл. 3.18
диссертации.

Оценку пункта 6 в таблице §7 нужно читать с учётом этого: там же и TSP.

---

## 5-тер. Что ещё полезного есть в MVCDIS для нас

Беглый осмотр показал:

- **`Models/alglib*.cs`, `dataanalysis.cs`** — в MVCDIS подключена библиотека **ALGLIB**, в которой
  есть готовая иерархическая кластеризация (та самая (3.9) с complete-linkage) и статистика.
  Это ответ на вопрос §4: не нужно писать кластеризацию с нуля, есть проверенный эталон, с
  которым можно сверять результаты.
- **`Models/Math.cs` (`NMath`)** — среднее, СКО, доверительный интервал по Стьюденту. Это формулы
  (3.5)–(3.7) и (3.10)–(3.11), которых у нас нет. 50 строк, переносятся почти дословно.
- **`Controllers/TreeController.cs`, `CompetenceController.cs`, `LevelController.cs`** — эталон
  работы с деревом компетенций и уровнями; полезно сверить нашу модель `Skill`/`SkillClosure`.
- **`Models/CompetenceManager.cs`** — практически пустой (весь код закомментирован), ценности нет.

⚠️ **Осторожно с MVCDIS как источником кода.** Это ASP.NET MVC 5 (.NET Framework) 2017 года:
контроллеры содержат SQL/бизнес-логику вперемешку с представлением, есть `SoftwareLocker.cs`,
`CheckLicense.cs`, `Encryption.cs`, `Keys.cs` — механизмы лицензирования, которые нам не нужны.
**Брать оттуда следует алгоритмы и формулы, а не архитектуру** — иначе пострадают DDD/SOLID,
которые вы просили не ломать. В репозиторий MVCDIS попал вместе с `.vs/`, `.suo`, `packages/` и
файлом БД `ASPNETDB.MDF` (10 МБ) — см. замечание в §9.

---

## 6. Что из главы 1 стоит взять в продукт

Глава 1 — про **онтологический подход к предметной области «управление»**: профессиональный
стандарт разложен на массив из **1827 единиц** (536 действий, 776 знаний, 677 умений) по
5 уровням организационной иерархии; показано, что цифровая трансформация видоизменяет 1222
единицы (67 %).

Три вывода для нашего проекта:

1. **Трёхчастная структура компетенции: действия / знания / умения.** У нас `Skill` — плоская
   сущность с уровнем 1–10. В первоисточнике это три разных типа единиц, и они по-разному
   оцениваются и по-разному устаревают. Добавить `Skill.UnitType` (action/knowledge/ability) —
   дёшево, а онтология сразу становится ближе к прототипу и к реальным профстандартам.
2. **Уровень иерархии как измерение роли.** `ProfessionalRole` стоит снабдить уровнем
   (руководитель организации / заместитель / руководитель подразделения / линейный / наблюдательный
   совет) — это готовая, обоснованная в диссертации таксономия для «LinkedIn будущего».
3. **Масштаб онтологии — 1827 единиц.** Это реалистичный ориентир объёма справочника навыков и
   аргумент в пользу того, что `SkillClosure` (замыкающая таблица) выбрана правильно: дерево
   такого размера с транзитивными запросами на рекурсивных CTE работать будет плохо.

---

## 7. Приоритет исправлений

| № | Что | Почему | Оценка |
|---|---|---|---|
| 1 | Отсечки в `Normalize` + учёт `MinScore` | **действующий дефект**: RoleFit завышается при MaxScore=5 | 0.5 дня |
| 2 | Разделить `CurrentLevel` на `LearningCurve` и `DecayedLevel` | **поведение обратно замыслу** | 1 день |
| 3 | Формула (3.29) `LearningRate` | без неё `k` берётся с потолка | 0.5 дня |
| 4 | Убывающая нормировка (3.3) | нужна для «чем меньше, тем лучше» | 0.5 дня |
| 5 | Иерархическая кластеризация (3.9) + z-score (3.5)–(3.7) | соответствие первоисточнику **+ снимает флейки-тест** | 1.5 дня |
| 6 | Матрица затрат (3.16)–(3.17) + **TSP методом ветвей и границ** (см. §5-бис) | самая ценная незакрытая идея, оживляет `DevelopmentPlan`; есть эталон в MVCDIS | 3–4 дня |
| 7 | `Skill.UnitType`, уровень иерархии у роли | онтология из главы 1 | 1 день |
| 8 | Перенести `NMath` (СКО, доверительный интервал) из MVCDIS | формулы (3.5)–(3.7), (3.10)–(3.11) | 0.5 дня |

Пункты 1–4 — правки внутри `MathService`, слои и паттерны не затрагиваются. Пункт 5 — там же.
Пункт 6 — новый `DevelopmentPathService` в Application отдельным файлом (не трогая `Services.cs`).
Пункт 7 — миграция EF.

---

## 8. Замечание по составу репозитория (важно)

Вместе с материалами в `master` попало то, чего в Git быть не должно:

- `MVCDIS/MVCDIS/App_Data/ASPNETDB.MDF` — **файл БД 10 МБ**;
- `MVCDIS/.vs/…/*.vsidx` — **индекс Visual Studio 3.7 МБ**, `.suo` — 696 КБ ×2 (бинарные,
  пользовательские);
- `MVCDIS/packages/` — восстанавливаемые NuGet-пакеты;
- PDF диссертации 6.9 МБ в корне репозитория (сам по себе уместен, но лучше в `docs/`).

Всего добавилось ~2690 файлов. Коммит `341a035` добавил `.gitignore`, но **уже закоммиченные
файлы он не убирает** — их нужно удалять явно (`git rm --cached`).

Отдельно: `neftviewer/neftviewer` записан как **gitlink (submodule) на коммит
`3914b901…`, но файла `.gitmodules` нет**. Поэтому папка приходит пустой, и я
**не могу прочитать NeftViewer** — `git submodule update` падает с «no submodule mapping found».

Чтобы я увидел NeftViewer, нужно либо добавить `.gitmodules` с URL репозитория:
```bash
git submodule add <URL-репозитория-neftviewer> neftviewer/neftviewer
git commit -m "add neftviewer as submodule"
```
либо, если это внутренний проект без отдельного remote — удалить gitlink и закоммитить
исходники как обычные файлы:
```bash
git rm --cached neftviewer/neftviewer
# скопировать исходники (без bin/obj/.vs) и git add
```
Пока этого нет, мои утверждения про «паттерн NeftViewer» опираются только на ваше описание в
задании и на `GenericRepository`/`UnitOfWork` в нашем проекте — сам первоисточник я не видел.

---

## 9. Оговорка о добросовестности

Формулы (3.12)–(3.26) я просмотрел, но детально не разбирал — они относятся к расчёту
приоритетов компетенций методом парных сравнений и к оценке уровня познания учебных материалов
(3.26)/(3.27-первая-нумерация). **Обратите внимание: в диссертации номер (3.27) использован
дважды** — на с. 100 для `Rimax = Wi·Kimax/100 %` и на с. 102 для ОДУ скорости обучения. В
разделе 3 выше разбирается второй вариант, поскольку именно он соответствует комментарию в коде.
Если вам нужен разбор блока (3.12)–(3.26) — скажите, сделаю отдельно.
