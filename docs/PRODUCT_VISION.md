# AI Social Network — каким должен быть проект

Дата: 2026-09-12. Составлено после чтения кода, диссертации MVCDIS (176 с.) и эталонной
реализации `MVCDIS/`.

Требование заказчика: **не перегруженный сложностями, но многофункциональный**. Ниже — как
это противоречие разрешить, а не сделать вид, что его нет.

---

## 1. Диагноз: проект уже перегружен, но недофункционален

Это главный вывод, и он неприятный. Цифры из репозитория:

| Метрика | Значение |
|---|---|
| Сущностей в домене / таблиц в БД | **58** |
| Из них **не используются нигде** в бизнес-логике | **26 (45 %)** |
| Endpoint'ов в API | 68 |
| Функций в клиентском `api.ts` | **10** |
| Страниц во фронтенде | **5** (Login, Users, Profile, Search, Notifications) |

Мёртвым грузом лежат: `Board`, `BoardColumn`, `BoardCard`, `Meeting`, `Organization`,
`Portfolio`, `Report`, `Review`, `KYCRequest`, `ConsentRecord`, `AuditLogEntry`,
`SubscriptionPlan`, `PlanSubscriber`, `Donation`, `RefundRequest`, `Language`, `UserLanguage`,
`Translation`, `ContentLanguage`, `UserFile`, `Attestation`, `DevelopmentPlan`, `PlanStep`,
`ForumPermission`, `GroupPermission`, `AnalyticsEvent`.

Обратите внимание на два имени в этом списке: **`DevelopmentPlan` и `PlanStep`**. Это ровно то,
ради чего писалась глава 3 диссертации — план развития компетенций. Таблицы созданы, кода нет.

**Вывод: перегруженность — не в том, что мало функций, а в том, что много каркаса без функций.**
«Многофункциональность» через добавление ещё десяти сущностей сделает только хуже. Настоящая
многофункциональность — когда 5–6 механик работают целиком и связаны друг с другом.

---

## 2. Что это за продукт (в одном абзаце)

> Профессиональная сеть, где у каждого специалиста есть **цифровой двойник** — измеримый профиль
> компетенций по онтологии MVCDIS — и **AI-агент**, который представляет владельца 24/7: отвечает,
> ищет подходящие заказы и исполнителей, ведёт первичные переговоры в заданных рамках. Договорённость
> агентов превращается в **сделку с эскроу по вехам**, а закрытая сделка — в **подтверждённую
> репутацию**. Репутация растёт от выполненных обязательств, а не от лайков.

Проверка на «зачем это нужно»: LinkedIn показывает, что человек *написал* о себе. Здесь система
показывает, что человек *сделал* (сделки) и что он *умеет* (аттестованные компетенции с
измеримым RoleFit). Это и есть отличие, ради которого стоит строить.

---

## 3. Принцип борьбы со сложностью: одна сквозная петля

Вместо «социальная сеть + биржа + мессенджер + LMS + CRM» — **один замкнутый цикл**, где каждый
шаг питает следующий:

```
   ┌─────────────────────────────────────────────────────────────┐
   │                                                             │
   ▼                                                             │
ПРОФИЛЬ ──▶ АГЕНТ ──▶ ПОИСК/МЭТЧИНГ ──▶ ПЕРЕГОВОРЫ ──▶ СДЕЛКА ──┤
компетенции  говорит   RoleFit подбирает   агент↔агент   эскроу   │
по MVCDIS    за вас    людей под задачу    в рамках      по вехам │
   ▲                                                             │
   │                                                             ▼
   └──────────── РЕПУТАЦИЯ ◀────────────────────────── ЗАКРЫТИЕ ──┘
        подтверждённая сделкой, повышает уровень навыка
```

**Правило проектирования: фича, не попадающая в эту петлю, не делается.** Канбан-доски, донаты,
мультиязычность, KYC — всё это «когда-нибудь», и их таблицы стоит убрать из активной модели
(см. §7), чтобы не создавать иллюзию, что они работают.

Многофункциональность достигается не количеством разделов меню, а тем, что **каждый узел петли
работает по-настоящему**: агент реально ведёт переговоры, эскроу реально держит деньги по вехам,
репутация реально считается по формулам.

---

## 4. Целевая архитектура (что меняется, что нет)

### Не меняется (работает, трогать не будем)
- 4 слоя DDD, зависимости вниз.
- `GenericRepository` + `IUnitOfWork`, коммит только в сервисе.
- EF-конфигурации в `Infrastructure/Configuration`, миграции в `Web`.
- Outbox → Kafka → `EventDispatcher`, SignalR-уведомления.
- JWT + BCrypt, FluentValidation, ProblemDetails.
- FTS на `tsvector`, LLM-модерация постов.

### Добавляется
| Что | Слой | Зачем |
|---|---|---|
| `ILlmProvider` + провайдеры | Application | развязать агента от Ollama, дать учёт токенов |
| `AgentTask` + `AgentWorker` | Domain + Web | асинхронный агент вместо блокирующего HTTP |
| `AgentMemory` + pgvector | Domain + Infrastructure | у агента появляется память |
| `Negotiation` + сага | Domain + Application | переговоры как процесс, а не переписка |
| `DevelopmentPathService` | Application | (3.16)–(3.17) + TSP — план развития |
| Исправленный `MathService` | Application | формулы по первоисточнику (см. аудит) |

Все шесть пунктов ложатся в существующие паттерны, ни один не требует ломать слои.

### Ключевое решение по мощностям (кратко, подробно — в `MODERNIZATION_PROPOSAL.md` §1)

Персональность агента ≠ персональная модель. Один общий инференс-пул + каскад «дешёвое сначала»:

```
L0 правило/шаблон (0 GPU)     ~35 %  ← «свободен во вторник?», «какая ставка?»
L1 семантический кэш          ~20 %  ← повторяющиеся вопросы
L2 малая модель, батч         ~40 %
L3 большая модель             ~5 %   ← сложные переговоры
```

Это снимает вопрос «каждому агенту своя видеокарта» без покупки железа.

---

## 5. Что именно реализуем: 6 блоков

Порядок выбран так, чтобы **после каждого блока продукт был целым**, а не «полфичи в проде».

### Блок 0. Гигиена (2 дня)
Фундамент, без которого остальное нельзя проверять.
- GitHub Actions CI: `dotnet build` + `dotnet test` + `npm run build`/`lint`.
- Чистка репозитория: `git rm --cached` для `ASPNETDB.MDF` (10 МБ), `.vs/`, `.suo`, `packages/`.
- Секреты из `appsettings.json` → user-secrets/env; JWT-ключ считать скомпрометированным.
- Rate limiting на `/api/agents/*` и `/api/social/posts`.

*Готовность: зелёный CI на каждый push.*

### Блок 1. Честная математика компетенций (3 дня)
Закрывает расхождения из `MVCDIS_FORMULA_AUDIT.md`.
- `NormalizeAscending`/`NormalizeDescending` с отсечками — формулы (3.2)/(3.3); учёт `MinScore`.
- Разделить `CurrentLevel` → `LearningCurve` (3.28)/(3.30) и `DecayedLevel` (затухание).
- `LearningRate` — формула (3.29).
- `ZScoreNormalize` (3.5)–(3.7), `NMath` (СКО, доверительный интервал) — перенос из MVCDIS.
- Иерархическая кластеризация complete-linkage (3.9) — **попутно чинит флейки-тест**.

*Готовность: RoleFit даёт корректные проценты; тесты сверены с табл. 3.4–3.5 и 3.17–3.18 диссертации.*

### Блок 2. План развития — оживляем мёртвые таблицы (4 дня)
Самая ценная незакрытая идея первоисточника.
- Матрица стоимостей (3.16)–(3.17).
- TSP методом ветвей и границ → **оптимальный порядок освоения компетенций** (эталон в MVCDIS).
- Наполнение `DevelopmentPlan`/`PlanStep` реальным содержимым.
- API `/api/competence/development-path`.

*Готовность: пользователь видит «чтобы дорасти до роли X, учи в порядке A→C→B, это 40 часов».*

### Блок 3. Агентский рантайм (5 дней)
Ядро продукта и ответ на проблему мощностей.
- `ILlmProvider` + `OllamaLlmProvider`; `AgentUsage` (токены, latency, стоимость).
- `AgentTask` + `AgentWorker` (BackgroundService): `send-message` → `202 Accepted` + taskId,
  ответ по SignalR.
- Каскад L0/L1: роутер намерений + семантический кэш на pgvector.
- Квоты по тарифу.

*Готовность: 100 одновременных диалогов не кладут API; ответ на типовой вопрос — мгновенно, без GPU.*

### Блок 4. Память агента и умный мэтчинг (4 дня)
- `AgentMemory` + pgvector, `IVectorSearchRepository` (по образцу `ISearchRepository`).
- RAG: агент отвечает, зная профиль, навыки, портфолио и историю сделок владельца.
- Гибридный поиск: `tsvector` (точность) + вектор (смысл) + RoleFit (пригодность).

*Готовность: агент отвечает «мой владелец делал похожий проект в 2025, бюджет был такой-то».*

### Блок 5. Переговоры → сделка → репутация (6 дней)
Замыкание петли.
- `Negotiation` + `NegotiationRound`, состояния: `draft → offer → counter-offer → agreed → rejected/expired`.
- Рамки от владельца (мин. ставка, дедлайн, макс. раундов) поверх `AgentPermission`.
- `agreed` → автосоздание `Deal` с вехами; эскроу уже есть.
- Взвешенная репутация: вклад сделки × сумма × давность × независимость контрагента (граф
  `Connection`), с сохранением разложения оценки.
- Закрытая сделка подтверждает навыки → `UserSkill.LastConfirmedAt`.

*Готовность: сквозной сценарий «агент нашёл заказ → договорился → эскроу → сдача → репутация выросла».*

### Блок 6. Фронтенд под петлю (5 дней)
Сейчас UI показывает 10 из 68 endpoint'ов — продукт невидим.
- «Мой агент»: настройка, права, лимиты, **лог решений** (витрина объяснимости).
- «Компетенции»: дерево навыков, RoleFit по ролям, план развития из блока 2.
- «Переговоры»: лента диалогов агентов, вмешательство владельца.
- «Сделки»: вехи, эскроу, закрытие.
- «Профиль»: репутация с разложением — из каких сделок сложилась.

*Готовность: демонстрация всей петли без Swagger и curl.*

---

## 6. Итог: ~29 рабочих дней

| Блок | Дней | Что появляется |
|---|---|---|
| 0. Гигиена | 2 | зелёный CI, безопасность |
| 1. Математика | 3 | корректные компетенции |
| 2. План развития | 4 | траектория обучения |
| 3. Агентский рантайм | 5 | масштабируемые агенты |
| 4. Память и мэтчинг | 4 | осмысленный агент |
| 5. Переговоры→сделка | 6 | замкнутая петля |
| 6. Фронтенд | 5 | видимый продукт |

Порядок не случаен: 1 нужен для 2, 3 нужен для 4 и 5, 6 показывает всё. Но **после любого блока
можно остановиться** — система остаётся связной.

---

## 7. Что убираем (чтобы не было перегруза)

26 неиспользуемых сущностей — это 45 % схемы, которые создают ложное впечатление готовности и
мешают читать модель. Предложение — **три группы**:

**А. Оставить как есть (нужны в петле, просто ещё не дошли руки):**
`Attestation`, `DevelopmentPlan`, `PlanStep`, `Portfolio`, `Review`, `AnalyticsEvent`,
`AuditLogEntry` — их наполняют блоки 2 и 5.

**Б. Перенести в «заморозку»** (миграция удаляет таблицы; сущности остаются в отдельной папке
`Domain/Entities/Backlog/` без регистрации в контексте):
`Board*`, `Meeting`, `Language`, `UserLanguage`, `Translation`, `ContentLanguage`, `UserFile`,
`Donation`, `RefundRequest`, `SubscriptionPlan`, `PlanSubscriber`, `KYCRequest`, `ConsentRecord`,
`ForumPermission`, `GroupPermission`, `Organization`, `Report`.

**В. Решить отдельно:** `SubscriptionPlan`/`PlanSubscriber` нужны, если монетизация в ближайших
планах (они связаны с квотами агента из блока 3) — тогда в группу А.

Это сокращает активную модель с 58 до ~35 сущностей. **Ничего не теряется** — код в Git, вернуть
можно в любой момент.

⚠️ Убирать таблицы — решение с последствиями. Нужно ваше явное согласие; по умолчанию я
**ничего не удаляю**.

---

## 8. Принципы, которых держимся

1. **Фича не в петле (§3) — не делается.** Лучший способ не перегрузить продукт.
2. **Каждый блок оставляет систему целой.** Не «начали пять фич, ни одной не закончили».
3. **Не плодим сущности впрок.** Урок 26 мёртвых таблиц: сначала сервис и API, потом таблица.
4. **Формулы — по первоисточнику**, расхождения документируются явно (как в аудите), а не
   «улучшаются» молча.
5. **Персональность агента — в данных, не в весах.** Один пул вычислений на всех.
6. **DDD/SOLID как есть.** Новое — отдельными файлами, старое не переписываем ради красоты.
7. **Каждый блок → тесты + запись в `IMPLEMENTATION_REPORT.md`.**

---

## 9. Вопросы, которые нужно закрыть до старта

1. **Согласны с петлёй (§3) как рамкой продукта?** Если приоритет другой (например, важнее
   группы/форумы, чем сделки) — план перестраивается.
2. **Заморозка 17 таблиц (§7, группа Б)** — делаем или оставляем всё?
3. **Монетизация в ближайшем горизонте?** От этого зависит судьба `SubscriptionPlan` и квот.
4. **`IMPLEMENTATION_REPORT.md` и два плана** — в репозитории их до сих пор нет. Без них не могу
   соблюдать правило 10.
5. **С какого блока начинаем?** Рекомендую 0 → 1: CI даёт мне возможность проверять работу,
   а математика — фундамент для всего остального.
